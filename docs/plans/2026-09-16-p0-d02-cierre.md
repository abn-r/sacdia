# Cierre P0 + D02 salto de tipo — Plan de implementación

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Alinear seeds, política de clase (edad luego secuencia), salto AV→CQ / CQ→GM automático y manual, aliases iOS, grants de assistant-admin, inscripción anual en admin y UI de sucesión, sin abrir D01 ni clases GM multianuales.

**Architecture:** El backend sigue siendo autoridad (permiso + rol + recurso + territorio + política). `NextClassResolver` decide clase y sección destino; `YearCutService` ejecuta el salto automático; `annual-continuations` cubre el manual y la lista. Admin/app/iOS se espejan al contrato. Spec: `docs/superpowers/specs/2026-09-16-p0-d02-cierre-design.md`.

**Tech Stack:** NestJS + Prisma + Jest (`sacdia-backend`); Next.js + Vitest (`sacdia-admin`); Flutter tests (`sacdia-app`); Swift Testing (`sacdia-admin-ios`).

---

## Mandato de ejecución

- Spec obligatoria: `docs/superpowers/specs/2026-09-16-p0-d02-cierre-design.md`
- `AGENTS.md` raíz. Rama actual. **Sin ramas, worktrees, builds, migrate Neon ni commits** salvo que el usuario lo pida en el chat.
- TDD en backend: test rojo → código → verde. No `Co-Authored-By`.
- Conservar R04 (directivo AV/CQ → GM no inscrito). No usar `cross_type_enrollment` para el salto.
- No listar solo `director-lf` en un `@GlobalRoles` nuevo.

```text
T1 seeds
  -> T2 NextClassResolver (C1 + D02 decisión)
  -> T3 annual-continuations lista/POST
  -> T4 year-cut automático
T2+T3+T4 -> T5 app copy D02 + recortar annualEnroll
T3 -> T6 admin continuaciones + C6 sucesión
T7 iOS aliases (paralelo a T2)
T8 assistant-admin SQL + catálogo
T1..T8 -> T9 docs canónicas
```

---

### Task 1: Roles en `prisma/seed.ts` y `roles.constants.ts`

**Files:**
- Modify: `sacdia-backend/prisma/seed.ts` (bloques GLOBAL ~L59-100 y CLUB ~L104-150)
- Modify: `sacdia-backend/src/common/constants/roles.constants.ts`
- Create: `sacdia-backend/prisma/seeds/README.md` (orden seed TS → permissions.sql → role-permissions.sql)
- Test: `sacdia-backend/src/common/constants/roles.constants.spec.ts` (crear si no existe)

**Step 1: Test de constantes**

```ts
import { CLUB_ROLE, GLOBAL_ROLE } from './roles.constants';

it('exports territorial, coordinator family and secretary-treasurer', () => {
  expect(GLOBAL_ROLE.DIRECTOR_LF).toBe('director-lf');
  expect(GLOBAL_ROLE.ASSISTANT_LF).toBe('assistant-lf');
  expect(GLOBAL_ROLE.DIRECTOR_UNION).toBe('director-union');
  expect(GLOBAL_ROLE.ASSISTANT_UNION).toBe('assistant-union');
  expect(GLOBAL_ROLE.DIRECTOR_DIA).toBe('director-dia');
  expect(GLOBAL_ROLE.ASSISTANT_DIA).toBe('assistant-dia');
  expect(GLOBAL_ROLE.ZONE_COORDINATOR).toBe('zone-coordinator');
  expect(GLOBAL_ROLE.GENERAL_COORDINATOR).toBe('general-coordinator');
  expect(CLUB_ROLE.SECRETARY_TREASURER).toBe('secretary-treasurer');
});
```

Run: `cd sacdia-backend && pnpm exec jest --runInBand --watchman=false src/common/constants/roles.constants.spec.ts`  
Expected: FAIL (exports missing)

**Step 2: Implementar constantes + seed.ts `createMany` skipDuplicates** con esos `role_name` / `role_category`. No inventar descripciones largas; una línea cada uno.

**Step 3: README de seeds** con el orden y la nota de que el SQL no INSERTA roles.

**Step 4: Jest verde.** No ejecutar `prisma db seed` contra Neon.

---

### Task 2: NextClassResolver — dos fases + salto de tipo

**Files:**
- Modify: `sacdia-backend/src/classes/next-class.resolver.ts`
- Modify: `sacdia-backend/src/classes/next-class.resolver.spec.ts`
- Modify: `sacdia-backend/src/classes/classes.module.ts` (inyectar `ClassAssignmentResolverService` si vive en common; exportarlo donde ya exista el provider)
- Modify: `sacdia-backend/src/common/services/class-assignment-resolver.service.ts` solo si hace falta exponer edad o resolver por `clubTypeId` ya existente

`ClassAssignmentResolverService` ya está en `src/common/services/class-assignment-resolver.service.ts` (`resolveClassIdForUserClubType`). Inyectarlo en `NextClassResolver`. El mock de Prisma debe incluir `users.findUnique` si el resolver de edad se llama con el mismo `tx`/`prisma`.

**Step 1: Reemplazar el test T5 (primera clase por display_order) y el D02 de AV agotado**

En `next-class.resolver.spec.ts`:

- T5 / B01: sin historial, usuario 13 años al `2026-01-01`, catálogo AV con `minimum_age` 10 y 13 → `class_id` de edad 13, **no** la de `display_order` mínimo si esa es edad 10.
- Conservar A12 (no investida avanza en el mismo tipo).
- Invertir `'D02 — exhausted AV catalog is configuration_error, not silent CQ enrollment'` **solo** cuando: birthday implica edad ≥ 10, `club_sections.findFirst` devuelve `SECTION_CQ`. Resultado: `kind: 'next_class'`, `crossed_type: true`, `club_section_id: 20`, clase CQ por edad.
- Nuevo: misma AV agotada, edad 9 → `configuration_error` `ANNUAL_CLASS_POLICY_UNRESOLVED`.
- Nuevo: AV agotada, edad 10, `findFirst` sección CQ `null` → mismo error.
- Conservar `'D02 — last GM class is configuration_error'` (no hay ministerio siguiente).
- Nuevo: Guía agotada (CQ último `display_order`), edad 16, sección GM → salto `crossed_type: true`.
- Nuevo: Guía agotada, `investiture_status: 'IN_PROGRESS'` → igual salta (B05).

Run: `cd sacdia-backend && pnpm exec jest --runInBand --watchman=false src/classes/next-class.resolver.spec.ts`  
Expected: FAIL (T5 y D02 AV ya no coinciden con el código viejo)

**Step 2: Implementar**

Pseudológica (no copiar ciega; adaptar al archivo):

1. Resolver sección origen y año (igual).
2. Historial regular del `club_type_id` origen, años con `end_date < start` destino.
3. Si no hay historial: `classAssignmentResolver.resolveClassIdForUserClubType` con ese tipo y año → `crossed_type: false`, misma sección. Si no hay clase: `ANNUAL_CLASS_POLICY_UNRESOLVED`.
4. Si hay historial: siguiente `display_order` mismo tipo. Si existe → misma sección, `crossed_type: false`.
5. Si no existe siguiente y el tipo es AV o CQ: buscar sección activa del tipo siguiente (`Conquistadores` / `Guías Mayores`) con el mismo `main_club_id`. Calcular edad al `start_date`. Bandas 10 / 16. Si ok: clase por edad en el tipo destino, `club_section_id` destino, `crossed_type: true`.
6. Si no: `configuration_error` `ANNUAL_CLASS_POLICY_UNRESOLVED`.

No escribir enrollments (el resolver sigue read-only; el test T9 debe seguir pasando: solo `findMany` en enrollments).

**Step 3: Jest verde** en `next-class.resolver.spec.ts`.

`ClassAssignmentResolverService` usa `Prisma.TransactionClient`. El resolver actual usa `PrismaService`. Pasar `this.prisma` como tx si el método acepta ambos, o añadir overload. No duplicar el cálculo de edad.

---

### Task 3: Lista y POST de continuaciones — candidatos de salto

**Files:**
- Modify: `sacdia-backend/src/annual-membership/annual-membership.service.ts` (`listContinuations` ~L113, `_enrollOneUser` si filtra solo CRA de esa sección)
- Modify: `sacdia-backend/src/annual-membership/annual-continuations.service.spec.ts`
- Revisar `AnnualMembershipPolicyService.listNotEnrolled`

**Step 1: Tests**

- GET sección CQ: usuario con última AV cursada (año cerrado), edad ≥ 10, no member active en CQ → aparece en la lista (`eligibility` ok, `suggested_class` resuelto, no `ANNUAL_CLASS_POLICY_UNRESOLVED`).
- POST CQ de ese user: `enrolled`, `crossed_type` reflejado si el outcome lo permite; CRA member active en CQ; enrollment tipo CQ. Origin AV no se reescribe histórico.
- POST de nuevo: `already_enrolled`.
- GET CQ no incluye al que ya es member active ahí.
- GET CQ no incluye R04 retornado GM que no es graduado CQ (el retornado GM sigue en lista **GM**, no en CQ).
- Edad 9: no aparece como elegible en CQ (o aparece `blocked` con código de política, no como éxito).
- `continueUsers` cuando `NextClassResolver` devuelve `club_section_id` distinto al `sectionId` del POST: **rechazar** si no coincide con el destino del POST (el actor inscribe en *esta* sección). Si el resolver apunta a otra sección, `blocked`.

**Step 2: Implementar** unión de `listNotEnrolled` local + graduados R14 del mismo `main_club_id` hacia `sectionId`.

Al inscribir, `_enrollOneUser` ya usa el resolver: si `crossed_type` y `club_section_id !== sectionId`, no inscribir en la sección incorrecta.

**Step 3: Jest** `annual-continuations.service.spec.ts` verde.

---

### Task 4: Year-cut inscribe el salto

**Files:**
- Modify: `sacdia-backend/src/year-cut/year-cut.service.ts` (`cutClub` ~L192: **después** de `activateScheduledPlans`, **antes** de `applyNotEnrolled` ~L288 — spec §5.2)
- Modify: `sacdia-backend/src/year-cut/year-cut.service.spec.ts`
- Posible: extraer un método compartido de enroll (no copiar `_enrollOneUser` a ciegas; inyectar `AnnualMembershipService` solo si no crea ciclo de módulos; si hay ciclo, extraer `TypeGraduationService` en `annual-membership` o `classes`)

**Step 1: Tests year-cut**

- B04: corte con usuario última AV, edad 10+, sección CQ activa → `member` active año nuevo en CQ + enrollment CQ. Contador nuevo opcional `typeGraduatesEnrolled` en `YearCutSummary` (si se toca el tipo, actualizar cron log y spec del cron).
- B05: misma sin INVESTIDO.
- B06: edad 9 → no enrollment CQ.
- B07: sin sección CQ → no CQ.
- B08: Guía + 16 + GM.
- B11: director CQ que retorna GM (R04) **no** usa este camino; sigue `returnedNotEnrolled`, sin clase.

**Step 2: Implementar** `applyTypeGraduates(tx, clubId, currentYear)` **antes** de `applyNotEnrolled`:

- Candidatos: expired/ended del club cuyo último enrollment regular agota el tipo AV o CQ.
- Llamar la misma decisión que `NextClassResolver.resolve`.
- Si `crossed_type && next_class`: writer de enrollment + CRA member active en destino (reutilizar writer). Idempotente.
- Quien salta **no** queda `member inactive` en origen por este corte (spec §5.2).
- No convertir R04 en graduado. `applyNotEnrolled` sigue cubriendo retornados GM.

**Step 3: Jest** `year-cut.service.spec.ts` (y e2e `test/annual-cycle-postgres.e2e-spec.ts` solo si el entorno de test local está definido; no Neon).

---

### Task 5: App — copy de política y recorte D01

**Files:**
- Modify: `sacdia-app/lib/features/members/presentation/views/annual_continuations_view.dart`
- Modify: `sacdia-app/lib/features/members/presentation/providers/members_providers.dart` (borrar o no exportar `AnnualEnrollNotifier` / `annualEnrollNotifierProvider` si ningún view lo usa; grep `annualEnroll` en `lib/`)
- Modify: strings easy_localization para `ANNUAL_CLASS_POLICY_UNRESOLVED` (política: catálogo/edad/sección, no “error”)
- Test: `sacdia-app/test/features/members/annual_membership_contract_test.dart` y tests de la view si existen

**Step 1:** `graft grep "annualEnroll"` / `rg annualEnrollNotifierProvider lib` — cero usos en UI. Quitar provider muerto o dejar `throw UnimplementedError` no; **eliminar** el notifier y el método del datasource **solo si** tests de contrato del 403 viven en backend. Si el test de app mockea el POST annual-enroll, cambiarlo a “no hay CTA”.

**Step 2:** Mapear código `ANNUAL_CLASS_POLICY_UNRESOLVED` a copy explícito.

**Step 3:** `cd sacdia-app && dart test test/features/members/` (sin `flutter build`).

---

### Task 6: Admin — continuaciones anuales + sucesión en sections-tab

**Files:**
- Create: `sacdia-admin/src/lib/api/annual-continuations.ts` (GET/POST `/club-sections/${id}/annual-continuations`)
- Create: `sacdia-admin/src/components/clubs/detail/annual-continuations-block.tsx`
- Modify: `sacdia-admin/src/components/clubs/detail/sections-tab.tsx` (montar bloque si `canCapability(..., "annual_continuations", "enroll")` o `canViewScreen`)
- Modify: `sacdia-admin/src/lib/auth/screen-catalog/screens/clubs.ts` — capability o pantalla `annual_continuations`: `permissions: [CLUB_MEMBERS_APPROVE]`, `surfaces: ["admin","app"]`; enroll = mismo permiso
- Modify: `sacdia-admin/src/lib/auth/screen-catalog/screen-catalog.test.ts` (o `screen-catalog.clubs.test.ts`)
- Modify: `sections-tab.tsx` — `SuccessionBlock` junto a `DesignationBlock`, visible con `canCapability(user, "clubs", "succeed_director")`
- Modify: `sacdia-admin/messages/es.json` (y en/fr/pt-BR) claves `clubs.detail.sections.successionHeading`, `successionHint`, `succeedNow`, `succeedConfirm`
- Test: `sacdia-admin/src/lib/clubs/actions.test.ts` ya cubre el action; añadir test de componente o del tab si el patrón del repo lo permite (`sections-tab` hoy puede no tener test: crear `annual-continuations-block.test.tsx` y `succession-block` mínimo)

**Sucesión UI:** form con `succeedClubSectionDirectorAction.bind(null, clubId, sectionId)`, hidden `current_assignment_id` del director activo del año vigente, confirm dialog. Copy: «Termina el director de este año y nombra al nuevo ahora. No es la preelección del año que viene.»

**Continuaciones:** tabla user + suggested_class + blocked_reason; POST lote. No llamar `annual-enroll`.

Run: `cd sacdia-admin && pnpm exec vitest run src/lib/auth/screen-catalog src/lib/clubs/actions.test.ts src/components/clubs/detail --reporter=dot`

No `next build`.

---

### Task 7: iOS aliases

**Files:**
- Modify: `sacdia-admin-ios/SACDIAAdmin/Core/Authorization/AuthorizationContext.swift`
- Create: `sacdia-admin-ios/SACDIAAdminTests/Core/AuthorizationContextAliasTests.swift`

**Step 1: Tests**

```swift
@Test func coordinatorAliasAdmitsDirectorLf() {
    let ctx = AuthorizationContext(permissions: [], globalRoles: ["director-lf"])
    #expect(ctx.canReviewEvidence)
}

@Test func exactMemberDoesNotReviewEvidence() {
    let ctx = AuthorizationContext(permissions: [], globalRoles: [])
    #expect(!ctx.canReviewEvidence)
}

@Test func assistantAdminSatisfiesAdminAlias() {
    let ctx = AuthorizationContext(permissions: [], globalRoles: ["assistant-admin"])
    #expect(ctx.roleGateSatisfied(["admin"]))
}
```

(Adaptar nombres al estilo del target. Extraer `roleGateSatisfied` o `expandRequiredRoles` como en `role-aliases.ts`.)

Mapa literal idéntico a `global-roles.guard.ts` líneas 25-48.

`canReviewEvidence` = gate `["admin","super-admin","coordinator"]` expandido. El permiso `validation:review` sigue en el API; iOS no debe fingir revisión sin permiso si ya tiene `can(_ permission)`. Si `canReviewEvidence` hoy es solo rol, mantener el mismo criterio **más aliases**, no añadir permiso aquí salvo que el store ya compruebe permisos.

**Step 2: Implementar mapa + expansión.**

**Step 3:** tests iOS del paquete Authorization.

---

### Task 8: assistant-admin grants + catálogo

**Files:**
- Modify: `sacdia-backend/prisma/seeds/role-permissions.seed.sql` (bloque nuevo **después** del wildcard de `admin`, ~L1748-1770)
- Modify: `sacdia-admin/src/lib/auth/screen-catalog/screen-catalog.test.ts` (ya hay casos assistant-admin)
- No copiar `NOT LIKE '%:delete'` completo si incluye `permissions:assign`. Excluir: `audit:read`, `%:delete`, `permissions:assign`, y las claves de crear/editar/borrar roles que el `rbac.controller` reserva a super-admin.

**Step 1:** Test SQL o spec de cleanup existente `permissions-cleanup.spec.ts` — afirmar que `assistant-admin` recibe `finances:read` (o una clave operativa) y **no** `permissions:assign`.

**Step 2:** INSERT…SELECT para `assistant-admin` como subconjunto. DELETE previo del role_id como los demás bloques.

**Step 3:** Catálogo: ningún `viewAny` solo-roles salvo API `@SkipPermissions`. Comentario en `insurance-expiring` ya correcto.

**Step 4:** `cd sacdia-backend && pnpm exec jest --runInBand --watchman=false src/common/seeds` y `cd sacdia-admin && pnpm exec vitest run src/lib/auth/screen-catalog`

---

### Task 9: Docs canónicas

**Files:**
- Modify: `docs/superpowers/specs/2026-09-08-inscripcion-anual-miembros-design.md` — nota al inicio: D02 salto de tipo cerrado en spec 2026-09-16; R05 exceptuado por R13; GM multianual sigue abierto
- Modify: `docs/features/clases-progresivas.md`, `docs/features/gestion-clubs.md`
- Modify: `docs/api/ENDPOINTS-LIVE-REFERENCE.md` (annual-continuations: candidatos de salto, `suggested_class`, `crossed_type` si el DTO lo expone)
- Modify: `docs/api/FRONTEND-INTEGRATION-GUIDE.md` (D02 salto ya no “no habilitar AV→CQ”; sí habilitar con R14)
- Modify: `docs/guides/conocimiento-sistema/05-matriz-roles-verificada.md` (LF en cola evidencias por alias coordinator — runtime 15-sep)
- Modify: `docs/guides/conocimiento-sistema/07-inscripcion-anual.md`, `08-clases-progresivas.md`
- Modify: `docs/audit/DECISIONS-PENDING.md`

No tocar PRESENTACIÓN PPTX. No afirmar desplegado.

---

## Verificación final (sin build)

```sh
cd sacdia-backend && pnpm exec jest --runInBand --watchman=false \
  src/classes/next-class.resolver.spec.ts \
  src/annual-membership/annual-continuations.service.spec.ts \
  src/year-cut/year-cut.service.spec.ts \
  src/common/constants/roles.constants.spec.ts

cd sacdia-admin && pnpm exec vitest run src/lib/auth/screen-catalog src/lib/clubs/actions.test.ts

cd sacdia-app && dart test test/features/members/
```

Criterios B01–B15 de la spec. D01 sigue 403. R04 intacto.

## Notas para el ejecutor

- Test e2e Postgres: solo con `SACDIA_TEST_DATABASE_URL` loopback `*_test`. Nunca `DATABASE_URL` de Neon.
- `graft build` al final si el usuario lo pide; no es bloqueante.
- Commits: no hacerlos en este plan hasta orden explícita. Si se piden: conventional commits por task (`feat(classes):`, `feat(admin):`, `fix(ios):`, `docs:`).
